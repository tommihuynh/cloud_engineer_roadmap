from server import Server

def create_server(hostname, ip, operating_system, status):
    """Use the class Server to add server information."""
    return Server(hostname, ip, operating_system, status)
    
